# Архитектура и стек (черновик)

## 1. Цель документа

Зафиксировать **разумный по умолчанию** стек и **альтернативы** — без догмы. Финальные выборы обновлять по мере прототипа.

## 2. Клиент

- **Язык:** **TypeScript** (строгий режим по мере введения `tsconfig`).
- **2D / «минимальный движок»:** варианты, хорошо сочетающиеся с браузером и 32×32:
  - **Phaser 3** — зрелый 2D, встроенные сцены, пулы, тайлмапы; **рекомендуемая отправная точка** для бродилки + простых врагов.
  - **PixiJS** — низкоуровневый рендер; больше ручного кода (сцена, ввод, коллизии) — смысл, если нужен минимальный вес.
  - **Кастом на Canvas 2D** — максимальная лёгкость, **максимум** ручной работы.

*Рекомендация агента: начать с **Phaser 3** + **Vite** для дев-сервера и бандла.*

## 3. Сервер (Node.js)

- **Назначение (минимум):** матчмейкинг **по ключу** сессии, **ауторитетное** хранение состояния (этап ретро, карточки, позиции/ивенты), **аутентификация** «знал ключ — попал в комнату» (не путать с личной учётной записью).
- **Протокол для игрового состояния:** **WebSocket** (например, **Socket.io** для быстрого старта или **ws** + свои сообщения) — **основной кандидат** для real-time 2D + ретро-логики.

**Почему не полагаться на WebRTC как на единственный «тонкий» сервер:**

- **DataChannel (WebRTC)** может снизить **серверный** трафик, но:
  - нужен **сигнальный** сервер и часто **TURN** (STUN не пробивает часть NAT);
  - **смешанная** топология (P2P + смена ведущего) усложняет **честный** game state и отладку;
  - для **ориентирного** размера сессии (**~16**; возможно **более** — смотри нагрузку) **один** небольшой **Node** на **WebSocket** обычно **достаточен** и проще в сопровождении.
- **Где WebRTC уместен позже:** если появятся **тяжёлые** P2P-фичи (низкая задержка кастомного дата-трафика) — пересмотреть **гибрид**: **WebSocket** для команд/состояния, **WebRTC** для выбранных потоков.

*Итог рекомендации:* **веб-сокет + авторитетный Node** для game state; WebRTC **не** как default без явного запроса и модели угроз/нагрузки.

## 4. Схема данных (логически, без схемы БД)

- **Сессия (room):** `inviteKey`, `leaderId` / `facilitatorId`, список `players[]`, `stage` (enum ретро), `cards[]`, `votes`, опционально `locationId`.
- **Сообщения:** JSON с полем `type` и `payload` (версионировать при эволюции API).

*Хранение: в памяти процесса — для MVP/нагрузочных тестов; **Redis/Postgres** — при необходимости персистентности и рестартов — вынести в отдельное решение.*

## 5. Сборка и репозиторий (монорепо — согласовано)

- **Monorepo:** **разделение** по папкам:
  - **`apps/client/`** — браузерный клиент (2D, UI ретро);
  - **`apps/server/`** — Node, WebSocket, состояние сессий.
- **Менеджер пакетов** и **workspaces** (`pnpm` / npm) — **зафиксировать** при инициализации `package.json` в `apps/*` (корневой workspace — TBD).
- **Линт / формат:** **ESLint** + **Prettier** — детали в `docs/CODE_STYLE.md` при подключении.

## 6. Деплой

- **docker-compose**; переменные через **`.env`** (не в git). См. `docs/DEPLOYMENT.md` и корневой `docker-compose.yml`.

## 7. GitLab

- Интеграции (CI, registry, тикеты) **подключатся позже**; подготовить **`.gitignore`**, **`.env.example`**, **доки** — уже сделано. После миграции в GitLab — обновить `docs/PROCESS.md` (URL, ветвление, review).

## 8. Тикеты (issues)

- **Ведение тикетов** в GitHub/GitLab из этого окружения **недоступно** (нет API учётки в контексте). После **вашей** публикации репо: создать эпики/тикеты по `docs/REQUIREMENTS.md` + этапам **с вашим согласованием** — зафиксировано в `AGENT.md`.
