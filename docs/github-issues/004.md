## Статус (2026-06-02)

**Закрыть тикет:** E1 смержен в `main` ([PR #12](https://github.com/VladikAN/rpg-retro/pull/12)).

- [x] `GET /health` → 200 `ok\n`
- [x] `POST /api/v1/rooms` → 201 JSON (`roomId`, `inviteCode`, `createdAt`, `paths`)
- [x] In-memory store (`server/internal/room/store.go`)
- [ ] `Store.GetByCode()` — **перенесено в [#6](https://github.com/VladikAN/rpg-retro/issues/6)** (нужно для WS в E3)

---

## Prerequisite
Close **#3** (E0) before starting. AGENTS.md — v0.1 epics are sequential (E0→E5).

## Текущее состояние кода
В коде **уже реализовано** (проверить, при необходимости дополнить):
- `POST /api/v1/rooms` → 201 JSON (`server/main.go`)
- `GET /health` → 200 `ok\n`
- In-memory store с `sync.RWMutex` (`server/internal/room/store.go`)
- `inviteCode`: 9 случайных байт → base64url → 12 символов

## Остаток (не блокирует закрытие E1)

1. **`Store.GetByCode(inviteCode string) (*Room, bool)`** — поиск комнаты по inviteCode.
   Нужен E3 для валидации при WS-подключении. Добавить в `store.go`, тест в `store_test.go`.
2. `go test ./...` — должно быть зелёным.

## Контракт ответа `POST /api/v1/rooms`

```json
{
  "roomId":     "uuid-or-ulid",
  "inviteCode": "abc123XYZ789",
  "createdAt":  "2024-01-01T12:00:00.000000000Z",
  "paths": {
    "g":    "/g/abc123XYZ789",
    "join": "/join/abc123XYZ789"
  }
}
```

Полный контракт: PROTOCOL.md → раздел «HTTP».

## DoD тикета (исторический чеклист)
- [x] `GET /health` → 200 `ok\n`
- [x] `POST /api/v1/rooms` → 201 JSON (все 4 поля, включая `paths`)
- [ ] `Store.GetByCode()` → см. **#6** реализован и покрыт тестом
- [x] `go test ./...` (на момент merge #12) зелёный
