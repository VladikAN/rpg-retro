# Minimal scaffold: extend when client and server services are added.
# All configuration via environment variables; use .env (not committed).

services:
  # app:
  #   build: .
  #   ports:
  #     - "${HTTP_PORT:-8080}:8080"
  #   environment:
  #     NODE_ENV: ${NODE_ENV:-development}
  #   env_file:
  #     - .env
  #   restart: unless-stopped

  placeholder:
    image: busybox:stable
    command: ["sh", "-c", "echo 'Replace placeholder with real services (client build + Node server)'; sleep infinity"]
    environment:
      NODE_ENV: ${NODE_ENV:-development}
