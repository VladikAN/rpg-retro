# Порт публикации приложения на хосте (см. docker-compose.yml)
APP_PORT=8080

# Окружение приложения (development / production)
APP_ENV=development

# --- Ниже — примеры; реальные имена зафиксировать при появлении сервера ---
# JWT_SECRET=replace-with-long-random-string
# DATABASE_URL=postgres://user:pass@db:5432/rpg_retro
