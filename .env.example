# Copy to .env and set values. Never commit .env.
# Used by docker-compose and local services.

# HTTP port exposed to the host (app / reverse proxy)
HTTP_PORT=8080

# Node environment
NODE_ENV=development

# Placeholder for future: database URL, JWT secret, TURN servers, etc.
# DATABASE_URL=
# JWT_SECRET=
